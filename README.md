#### Expenses Tracker - Flutter

Building a minimal expense tracker to try out widget positionining.

#### Development
Run Project on web mode
```shell
flutter run -d chrome
```

Use `Ctrl + F5` to launch application in debug mode using **vscode**
